import axios from "axios"; const API_BASE = import.meta.env.VITE_API_BASE || "http://localhost:8000/api/medications/"; export async function getMedications(token) { const res = await axios.get(API_BASE, { headers: { Authorization: `Bearer ${token}` }, }); return res.data; } export async function createMedication(data, token) { const res = await axios.post(API_BASE, data, { headers: { Authorization: `Bearer ${token}` }, }); return res.data; } export async function updateMedication(id, data, token) { const res = await axios.patch(`${API_BASE}${id}/`, data, { headers: { Authorization: `Bearer ${token}` }, }); return res.data; } 